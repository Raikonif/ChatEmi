<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--<item android:id="@+id/menu_new_message"
        android:title="@string/new_message"
        android:icon="@drawable/ic_image_menu"
        app:showAsAction="ifRoom"/>-->

    <item android:id="@+id/menu_profile_view"
        android:title="@string/profile_view"
        app:showAsAction="never"/>

    <item android:id="@+id/menu_sign_out"
        android:title="@string/sign_out"
        app:showAsAction="never"/>

    <item android:id="@+id/menu_about_us"
        android:title="@string/about_to"
        app:showAsAction="never"/>

</menu>